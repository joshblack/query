# Project configuration

- **Repository:** `joshblack/query`
- **Project owner:** `joshblack`
- **Project number:** `18`
- **Project URL:** https://github.com/users/joshblack/projects/18
- **Status field:** built-in `Status`

Use `gh project` commands with owner `joshblack` and project number `18`.

The GitHub CLI token must include the `read:project` and `project` scopes.
